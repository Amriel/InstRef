# Pinned on purpose: an upstream release must not break our build without a
# change on our side. Bump deliberately, run the suite, then commit.
PySide6==6.11.2
instagrapi==2.18.18
browser-cookie3==0.20.1
requests==2.33.1
mutagen==1.48.1
piexif==1.1.3
opencv-python-headless==4.13.0.92
imagehash==4.3.2
