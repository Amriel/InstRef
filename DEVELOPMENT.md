# InstRef — документ розробки

Внутрішній опис проєкту: як він влаштований, чому влаштований саме так, які
помилки вже зроблені й чим вони закінчились. `README.md` — для того, хто
користується; цей файл — для того, хто продовжує писати код.

Версія на момент запису: **2.4.1** · 215 тестів · ~12 100 рядків коду.

> **Цей документ оновлюється разом із кодом — завжди, у тій самій зміні.**
> Він описує застосунок таким, яким той є зараз. Документ, який відстав від
> коду, гірший за його відсутність: відсутньому не вірять, а застарілому
> вірять. Правила й чек-лист — у `CLAUDE.md`, розділ 14 нижче — коротко.

---

## 1. Що це

Застосунок збирає збережені й пролайкані пости Instagram, кладе їх у папку та
в бібліотеку Eagle, вшиває опис і автора всередину самих файлів, а візуальна
модель у LM Studio дивиться на кадри й пише опис та теги з контрольованого
словника.

**Межа продукту:** InstRef — конвеєр, а не бібліотека. Instagram → фільтр →
опис → теги → Eagle. Базою є Eagle: він зберігає власну копію кожного файлу,
у ньому шукають і ним діляться. Тому тут немає і не буде власного пошуку,
власної вкладки-галереї чи довгого зберігання вихідників — папка завантажень
лише перевалка, яку застосунок уміє прибирати за собою.

Три речі, які визначили архітектуру:

1. **Instagram не має публічного API для збереженого.** Використовується
   `instagrapi` — обгортка над приватним мобільним API. Звідси: sessionid
   замість пароля, паузи між сторінками, обережність із кількістю запитів.
2. **Eagle на кожен імпорт копіює файл.** Це не бібліотека посилань. Тому
   «додати той самий пост ще й у папку лайків» = друга копія на диску, а не
   друга полиця. Півпроєкту крутиться навколо цієї властивості.
3. **Користувач працює наосліп.** Він не бачить логів, поки не відкриє їх.
   Тому головний принцип нижче.

---

## 2. Головний принцип: жодних тихих відмов

Це не стиль, а наслідок болю. Кожна друга проблема в цьому проєкті виглядала
однаково: застосунок нічого не зробив і нічого не сказав.

| Що мовчало | Як виглядало для користувача |
|---|---|
| Кукі Chrome зашифровані App-Bound Encryption | «сесії є, а воно не бачить» |
| Eagle запущений, але імпорт вимкнено галочкою | «0 файлів пішло в ігл» |
| Візуальна модель не піднялась через прапорець | «просто додалось в ревью, не відправилось моделі» |
| Запуск за розкладом упав без вікна | тиша, ніяких файлів |
| Порожній опис збережено й заблокував повтор | «на частину відео взагалі опису не робить» |

**Правило:** якщо гілка коду веде до «нічого не робимо» — там має бути
`self.log(...)` з причиною. Не `return None`. Приклади в коді:
`_setup_vision()`, `_setup_eagle()`, `_describe()`, `describe_library()`.

Друге правило звідти ж: **порожній результат не зберігається**. Порожній опис
у `ai_meta` виглядав як «вже описано» і назавжди закривав повторну спробу.

---

## 3. Карта модулів

```
igsaved/
├── config.py       Config (82 поля) + JSON + міграції; session.json під DPAPI
├── state.py        SQLite: памʼять про завантажене, ревʼю, описи, Eagle, бекапи
├── instagram.py    instagrapi: підбірки, ітерація постів, пост за URL, класи помилок
├── downloader.py   HTTP-завантаження з ретраями й лімітом розміру
├── naming.py       читабельні імена файлів із підпису поста
├── tagging.py      опис/автор/посилання ВСЕРЕДИНУ mp4 (mutagen) і jpg (piexif)
├── classify.py     правила «мем / арт / реклама» за текстом і акаунтом
├── frames.py       кадри з відео (за сценами), бюджет від тривалості, dHash
├── vision.py       клієнт LM Studio, інструкція, розбір відповіді, hash промпта
├── taxonomy.py     контрольований словник тегів + застосування його кодом
├── transcribe.py   голос за кадром → текст (faster-whisper, опційно)
├── eagle.py        локальний API Eagle
├── lock.py         міжпроцесний замок: один прохід за раз
├── updates.py      перевірка нової версії через GitHub Releases (+ assets, zipball)
├── updater.py      оновлення однією кнопкою: інсталятор або архів вихідників
├── sync.py         оркестратор одного проходу + run_urls()
├── maintenance.py  обслуговування: дозалив, описи, дублікати, ретро-нормалізація
├── scheduler.py    schtasks + автозапуск Windows
├── session.py      пошук sessionid у кукі браузерів
├── status.py       last_run.json — щоб GUI знав про падіння фонового запуску
├── cli.py          запуск без вікна: джитер, ротація логів, підсумок-сповіщення
└── ui/
    ├── main_window.py  бічна панель із 8 розділів, композиція сторінок, обробники
    ├── pages.py        PagesMixin: блоки налаштувань і дій за темами (_sec_*)
    ├── widgets.py      спільні дрібні віджети: _section, _subtabs, _hint…
    ├── review_tab.py   картки з кадрострічкою, редагуванням, клавіатурним трияжем
    ├── tag_dialog.py   пропозиції до словника тегів
    ├── workers.py      QThread на кожну довгу операцію
    └── theme.py        стилі

tests/test_core.py  двигун, база, модель, обслуговування
tests/test_ui.py    вікно й вкладка перегляду безголово (offscreen)

entry.py            точка входу зібраного .exe: GUI або --sync
InstRef.spec        збірка PyInstaller
installer/          Inno Setup
.github/workflows/  тести на кожен пуш, збірка на тег
```

Залежності односторонні: `ui/ → sync/maintenance → state/eagle/vision → config`.
`sync.py` не знає про Qt — тому той самий двигун працює і в `cli.py`.

---

## 4. База даних

SQLite, WAL. Схема в `state.py::SCHEMA`.

| Таблиця | Ключ | Навіщо |
|---|---|---|
| `media` | `pk` | статус поста + `attempts` / `last_error` (облік невдач) |
| `files` | `path` | що саме лежить на диску, з номером слайда |
| `membership` | `(media_pk, collection_pk)` | у яких підбірках бачили пост |
| `eagle_items` | `(media_pk, collection_pk)` | що вже пішло в бібліотеку + `item_id` Eagle |
| `collections` | `pk` | назви й лічильники підбірок |
| `review` | `media_pk` | черга на рішення + `source`: rules / model |
| `ai_meta` | `(media_pk, idx)` | опис, теги, `screen_text`, `transcript`, `prompt_hash` |
| `tag_candidates` | `tag` | теги поза словником: скільки разів модель просила |
| `fingerprints` | `(media_pk, idx, hash)` | dHash кадрів — пошук репостів |
| `exemplars` | `(media_pk, idx)` | описи, схвалені як зразок (few-shot) |
| `agreement` | `media_pk` | що модель запропонувала / що вирішила людина |
| `meta` | `key` | ключ/значення: `cooldown_until`, `liked_top_pk`, `last_backup`… |
| `runs` | autoincrement | історія запусків |

### Статуси `media` — головна логіка «не качати двічі»

```
pending  → щойно побачили, ще не завантажили
done     → завантажено, файли мають бути на диску
archived → завантажено, файли СВІДОМО прибрані чисткою папки
skipped  → свідомо не качаємо (мем / відхилено в ревʼю)
failed   → не вдалось, спроби ще є (attempts < max_post_attempts)
gave_up  → не вдалось N разів — більше не чіпаємо; is_known → True
```

`gave_up` існує, бо видалений автором чи битий пост інакше повторювався б
кожен прохід назавжди, витрачаючи запити до Instagram нізащо. Список таких
постів показується в кінці проходу й у «Додатково», звідки їх можна
повернути в чергу (`retry_given_up`).

`is_known(pk)`:

* `archived` → **True без перевірки диска**. Файлів навмисно немає, і питати
  диск означало б качати все заново після кожної чистки.
* `done` → True тільки якщо файли реально на місці. Це запобіжник на випадок,
  коли користувач випадково стер файл.

Різниця між цими двома рядками — те, заради чого існує кнопка «Очистити папку
завантажень»: вона видаляє файли й ставить `archived`, зберігаючи памʼять.

### Міграції

`CREATE TABLE IF NOT EXISTS` не змінює вже створену таблицю. Тому
`_add_missing_columns()`:

* дописує нові стовпці через `ALTER TABLE` (список `State.MISSING`);
* перезбирає таблицю, коли змінився первинний ключ (`ai_meta` переїхала з
  `(media_pk)` на `(media_pk, idx)` — ALTER цього не вміє). Перезбирання
  йде **до** доливання стовпців — інакше нові стовпці додались би до старої
  таблиці й зникли разом із нею.

Перед міграцією (`_needs_migration()`) робиться резервна копія
`backups/state_<дата>_pre-migration.db` через sqlite backup API — узгоджена
навіть під WAL. Окремо раз на тиждень `backup_if_due()` на початку проходу;
тримається `BACKUP_KEEP = 5` останніх.

Міграції перевірені на копії реальної бази (629 постів, 189 описів), а не
лише на порожній — саме там вилазять несподіванки.

---

## 5. Потік синхронізації

```
run()
 ├─ cooldown_until?  (примусова пауза після «зачекай») → стоп, навіть force
 ├─ cooldown_left()  (межа між проходами)             → стоп, якщо не force
 ├─ RunLock(sync.lock)                                → стоп, якщо інший процес
 └─ _run_locked()
     ├─ backup_if_due()
     ├─ ig.connect(sessionid)
     ├─ _setup_eagle()              ← каже вголос, якщо Eagle є, а імпорт вимкнено
     ├─ _setup_vision()             ← каже вголос, якщо модель пропущено
     ├─ для кожної підбірки:
     │   _sync_collection(col)      ← для лайків: курсор liked_top_pk
     │    └─ для кожного поста:
     │        ├─ ліміт перегляду?            → стоп
     │        ├─ is_skipped / is_known?      → пропуск (+ дозалив у Eagle)
     │        ├─ _judge(media, col)          → лише для ЛАЙКІВ
     │        │   ├─ classify.classify()     → правила за текстом
     │        │   └─ _ask_vision()           → лише для сумнівних
     │        └─ _process_media()            ← виняток → _note_failure()
     │            ├─ collect_assets → завантаження (або переїзд із _cache)
     │            ├─ _fingerprint()          → dHash; схоже на наявне → перегляд
     │            ├─ _describe()             → опис і теги, якщо увімкнено
     │            ├─ _embed()                → метадані всередину файлу
     │            ├─ _preview_from_file()    → превʼю з кадру, без CDN
     │            └─ черга Eagle  АБО  черга перегляду
     ├─ _cleanup_imported()          ← заодно вивчає item_id елементів Eagle
     ├─ _describe_backlog()          ← N описів для старої бібліотеки
     └─ _report_given_up()
RateLimited будь-де → _enter_rate_limit(): весь прохід стоп, cooldown_until = +24 год
```

`run_urls(urls)` — другий вхід у той самий конвеєр: конкретні пости за
посиланням (`media_from_url`), псевдо-підбірка «За посиланням» (`MANUAL_PK`),
без обходу й без межі між проходами, але під замком і примусовою паузою.

### Темп запитів — головний захист акаунта

`instagrapi` працює через приватне мобільне API, і Instagram виявляє
автоматизацію. Виявляє не за обсягом, а за **темпом**: рівні короткі інтервали
й повторні входи виглядають як скрипт, бо людина так не гортає.

Після реального попередження від Instagram («підозріла активність, підозра в
автоматизації») зроблено три речі:

1. **Запобіжник частоти.** `min_hours_between_runs` (типово 6). `run()`
   відмовляється працювати, якщо попередній прохід був щойно — і каже про це
   вголос. Запуск за розкладом просто пропускається; ручний питає
   підтвердження, бо людина має право наполягти свідомо.
2. **Перевикористання сесії.** `connect()` спершу пробує профіль із
   `device.json`: якщо збережений `sessionid` того самого акаунта й у профілі
   є `authorization_data`, робиться один дешевий `account_info()` замість
   повного `login_by_sessionid` (кілька запитів). Десяток свіжих входів на
   добу був найпомітнішим слідом.
3. **Спокійніші паузи.** 2–5 с → 8–15 с між сторінками, 0.4 → 1.0 с між
   завантаженнями. Міграція піднімає їх тим, хто не редагував.

4. **Зупинка на «зачекай».** `instagram.classify_error()` упізнає
   `PleaseWaitFewMinutes` / 429 / `feedback_required` за назвою й текстом
   винятку (класи instagrapi не імпортуються — їх перейменовують між
   версіями) і кидає `RateLimited`. Прохід зупиняється **цілком**, у `meta`
   пишеться `cooldown_until` (+`rate_limit_cooldown_hours`, типово 24), і цю
   паузу не обходить навіть ручний запуск. Продовжувати після такої
   відповіді — підтвердити підозру. `SessionDead` (login/challenge) окремо:
   планові проходи стають зі статусом `no_session` і сповіщенням.
5. **Один прохід за раз.** `lock.RunLock` — файл `sync.lock` із pid у теці
   застосунку. Вікно і планувальник більше не ходять в Instagram одночасно.
   Живість власника перевіряється, тож після краху замок не блокує назавжди.
6. **Джитер.** Плановий прохід починається у випадковий момент у вікні
   `schedule_jitter_minutes` (типово 20): рівно о 09:00:00 щодня — підпис
   планувальника.

Чого тут свідомо **немає** і не буде: підробки відбитків пристроїв,
імітації людських патернів, ротації проксі. Це вже обхід захисту платформи,
а не акуратне користування. Єдиний чесний важіль — звертатись рідше.

### Ліміти обходу

`max_items_per_run` рахує **завантажене**. У стрічці лайків, повній мемів, він
не спрацьовував ніколи — відсіяне не збільшує лічильник, і обхід тягнувся через
усю історію. Тому окремо є `liked_scan_limit` / `scan_limit`, які рахують
**переглянуте**.

Для лайків є ще **курсор**: `meta.liked_top_pk` — найсвіжіший пост попереднього
проходу. Стрічка лайків іде від нових до старих, тож дійшовши до нього, ми
бачили все нове. Вікно «останні 50» мовчки губило те, що за межею; з курсором
межа піднімається до `LIKED_CURSOR_CEILING` (1000) як запобіжник.

### Кадри й `_cache`

Обкладинка reels — часто чорний кадр або титр. Судити за нею про ролик було
головним джерелом помилок класифікації. Тому:

1. Сумнівний ролик качається наперед у `<downloads>/_cache/<pk>.mp4`.
2. `frames.extract()` бере N кадрів. Скільки — `frame_budget()`: приблизно
   один на `vision_seconds_per_frame` секунд (типово 5), не менше
   `vision_frames`, не більше стелі. Де — `scene_positions()`: ролик
   семплюється дрібними кадрами, різниця HSV-гістограм між сусідніми
   показує склейки (медіана + 4·MAD), кадри беруться з середини найдовших
   сцен, майже чорні (`DARK_LIMIT`) минаються. Не вийшло — рівномірно,
   як раніше (`even_positions`).
3. Модель бачить усі кадри одним запитом.
4. Якщо пост схвалено — файл **переїжджає** з `_cache` на місце
   (`Path.replace`, той самий диск, миттєво). Другого завантаження немає.

`_prefetch: Dict[url, Path]` звʼязує ці кроки; `_download_or_reuse()` перевіряє
кеш першим ділом. Хвости прибирає `_clear_cache()` у `finally`.

Кількість кадрів — 1..60. Понад 12 кадри автоматично здрібнюються
(`frames.side_for()`), бо 60 кадрів по 640 px — це мегабайти base64 й десятки
тисяч токенів контексту.

---

## 6. Черга перегляду

Спершу черг було дві — «Ревʼю» й «Рішення моделі». Виявилось, що це один
інструмент, розрізаний навпіл: дії над обома чергами однакові, а дві вкладки з
тими самими двома кнопками лише змушували перемикатись. Злито в одну.

Походження не зникло — воно лишилось у колонці `review.source` і показується
позначкою на картці:

| `source` | Позначка | Що це означає |
|---|---|---|
| `rules` | немає | правила не змогли вирішити, рішення цілком за тобою |
| `model` | ◆ модель: качати | схвалено, файл в основній теці, чекає згоди на Eagle |
| `model` | ◆ модель: пропустити | відсіяно, файл у `_review`, буде видалений |

Механіка спільна: **Залишити** = перенести з `_review` до основної теки +
відправити в Eagle; **Не качати** = видалити файл + `mark_skipped`. Для рішень
моделі змінюються лише написи («Врятувати» / «Погоджуюсь»).

Кнопка «Погодитись з моделлю» виконує її пропозиції гуртом і свідомо **не
чіпає** записи з `source = rules`: модель їх не бачила, і вирішувати за неї
було б підміною.

**Чому відсіяне моделлю все одно завантажується.** Ролик уже лежить у `_cache`
після перегляду кадрів — покласти його в `_review` коштує нуль трафіку. Зате
помилкове «мем» стає видимим і його можна врятувати. Помилкове «мем» коштує
втраченого поста, помилкове «арт» — зайвого файлу, тому в списку відсіяне йде
першим.

У Eagle не потрапляє нічого, що чекає рішення: `is_pending_review()`
перевіряється і в синхронізації, і в дозаливі.

**Картка** (2.3.0): кадрострічка з 4 кадрів за сценами (кешується в
`_thumbnails/<stem>_strip.jpg`), опис і теги моделі в редагованих полях
(автодоповнення зі словника), кнопка ★ «гарний опис» → `exemplars` → few-shot
в інструкції. Правки пишуться в `ai_meta` при рішенні (`save_edits`), і саме
звідти їх читає імпорт у Eagle — тож у бібліотеку йде вже виправлене.

**Клавіатура:** стрілки / hjkl — курсор, `Y` — залишити, `N` — не качати,
`O` — відкрити файл, `S` — зірка. У полі опису клавіші не перехоплюються
(`Esc` повертає фокус вкладці).

**Згода з моделлю** — таблиця `agreement`: що модель запропонувала і що
вирішила людина, з категорією. Показується над сіткою: «Згода з моделлю: 87%
з 40 рішень · art 92% (25) · meme 75% (12)». Це і є калібрування: confidence у
qwen3-vl-4b — константа 0.95, і поріг `vision_min_confidence` фактично мертвий.

Ще один шлях у чергу — **можливий репост**: `_fingerprint()` рахує dHash
кадрів нового файлу й шукає серед `fingerprints` найближчий (Гемінг ≤
`dupe_max_distance`, типово 8). `dupe_action`: review (у чергу з підказкою
«схоже на @user … url»), import, skip, off.

---

## 6a. Структура вікна

До 2.4 вікно мало 4 верхні вкладки й окремі «Налаштування» із семи розділів,
а індикатори й кнопки обслуговування додавались туди, де було місце. У 2.4 —
бічна панель, де **один розділ — одна тема**, а всередині розділу підвкладки
за підтемами (`widgets._subtabs`). Правило розкладання: налаштування живе
там, де живе дія, якої воно стосується.

| Розділ | Підвкладки | Що там |
|---|---|---|
| Огляд | — | картки стану Сесія / Eagle / Модель, банер збою, повідомлення про оновлення, останній запуск, черга, журнал, «Синхронізувати» |
| Синхронізація | Підбірки · Що качати · Обхід · Лайки та фільтр | таблиця підбірок із галочками, «За посиланням», папка/типи/шаблон, ліміти обходу, фільтр мемів і списки акаунтів |
| Перегляд | — | `ReviewTab` |
| Модель | Підключення · Кадри · Рішення · Інструкція · Словник | LM Studio, describe/transcribe, кадри й сцени, категорії/впевненість/glance, промпт, словник + пропозиції + звіт + нормалізація |
| Eagle | Підключення · Теги й нотатка · Прибирання й дублікати · Бібліотека | API/тека/один елемент, теги, cleanup/dupe_action/пошук дублікатів, бекфіл описів і дозалив |
| Акаунт | Сесія · Захист · Розклад | sessionid, паузи/кулдаун/429/джитер/спроби, планувальник і поведінка |
| Обслуговування | Файли й база · Мережа | перейменування, відкладені пости, чистка, забути історію, теки логів/бекапів/застосунку, ярлик, скидання; таймаут/проксі |
| Про застосунок | — | версія, спосіб запуску, «Перевірити оновлення», **«Оновити до X»**, опис релізу, посилання |

Код: `main_window._page_*` збирає сторінку з блоків `pages.PagesMixin._sec_*`;
імена віджетів (`self.sp_jitter` тощо) не змінились, тож `_load_config_into_ui`
і `_collect_ui_into_config` лишились як були. Перехід між розділами
**зберігає налаштування сам** (`_on_page_changed`) — забуте «Зберегти» коштувало
не одному налаштуванню. Кнопка «Зберегти» лишилась у нижній смузі для звички.

### Оновлення однією кнопкою

`updater.update(latest)` обирає шлях за `config.FROZEN`:

* **інсталятор**: `updates.installer_asset()` знаходить `InstRef-Setup-*.exe`
  серед assets релізу → `download()` у %TEMP% → запуск із
  `/SILENT /CLOSEAPPLICATIONS /RESTARTAPPLICATIONS` → застосунок виходить;
* **вихідники**: `zipball_url` тегу → `apply_source_archive()` переносить файли
  поверх проєкту, минаючи `KEEP` (config.json, session.json, state.db…,
  taxonomy.json) і `KEEP_DIRS` (.git, .venv, logs, backups, downloads) і
  зводячи `.bat` до CRLF → `install_requirements()` тим самим інтерпретатором →
  `restart_from_source()` (pythonw.exe, якщо є) → вихід.

`UpgradeWorker` веде прогрес у «Про застосунок»; `UpdateWorker` раз на добу
(`meta.last_update_check`) заповнює і банер на «Огляді», і кнопку.

## 7. Візуальна модель

LM Studio, OpenAI-сумісний HTTP. Ніяких нових залежностей.

### Що повертає модель

```json
{"category": "art", "confidence": 0.87,
 "description": "One paragraph, English, under 80 words",
 "on_screen_text": "Step 1: bake normals | Houdini 20",
 "tags": ["close-up", "neon-lit", "..."],
 "why": "max 8 words"}
```

`category` — рішення про полицю (мем/арт/реклама/гра/інше), окреме від тегів.
`on_screen_text` — текст, вшитий у кадри (підписи, назви плагінів, кроки):
у туторіалах саме за ним потім шукають. Іде в анотацію Eagle рядком
«On screen: …» і в `ai_meta.screen_text`.

**Контекст запиту** (після інструкції): акаунт, підпис, підказка про підбірку
(`collection_hint`: пост із «Houdini»/«Tut» — туторіал, описувати техніку, а
не картинку), транскрипт голосу за кадром, якщо є. **Зразки** — до 3
описів із `exemplars` через плейсхолдер `{examples}`: «match their register,
length and level of detail».

**Відбиток інструкції.** `vision.prompt_hash(template, model)` — sha1[:12]
тексту інструкції й назви моделі — пишеться в `ai_meta.prompt_hash`. Після
зміни інструкції «Переписати застарілі» (`describe_library(only_stale=True)`)
переописує лише те, що описано іншою; «Іншою моделлю…» (`model_override`)
переописує все сильнішою моделлю, прибираючи старі теги моделі й лишаючи
ручні.

### Розбір відповіді

`parse_answer()` витримує: обгортку ```` ```json ````, текст навколо, фігурні
дужки **всередині опису** (`_first_json_object()` рахує дужки з урахуванням
рядків — регекс тут не годиться), відсутній JSON узагалі (тоді шукає назву
категорії словом), невідому категорію (опис і теги все одно повертає — вони
не винні).

### Контрольований словник

Головна причина, чому бібліотека перестає шукатись, — синоніми: `3d-render`,
`3drender`, `render`, `3d` — чотири різні теги для Eagle.

`taxonomy.py` — 29 категорій, 367 тегів, 51 синонім, 8 груп несумісних тегів.
Списки підставляються в інструкцію через `{taxonomy}`.

**Ключове рішення: словник не просять дотримувати — його застосовують.**
`Taxonomy.normalize()` після відповіді моделі:

* приводить до вигляду (`Close-Up` → `close-up`);
* мапить промахи (`skiing` → `sport-action`, `goldfish` → `fish`);
* викидає все, чого немає у списках;
* прибирає несумісні пари (`handheld` + `locked-off` — лишається перший);
* тримає ліміт на категорію;
* для нерухомої картинки не застосовує категорії руху й монтажу;
* дописує `autotagged` останнім.

Інструкцію модель порушує; цю перевірку — ні.

**Словник росте, але свідомо.** Відкинутий тег не зникає — `tag_candidates`
рахує його. Після N повторів (типово 5) він зʼявляється в діалозі
«Пропозиції до словника»: видно, скільки разів модель просила, і одним кліком
тег додається в обрану категорію. Так словник наростає по реальному контенту,
а не по фантазіях моделі.

Файл `taxonomy.json` лежить поруч із `config.json`, правиться руками і **не
входить у zip оновлення** — інакше апдейт затирав би правки. Вбудовані
синоніми при цьому діють і під старим файлом: `from_dict` кладе
`DEFAULT_ALIASES` підкладкою, записи з файлу мають перевагу. Інакше нові
промахи, які ми навчились мапити, ніколи не дійшли б до того, хто зберіг
словник раніше.

**Ретро-нормалізація** (`maintenance.normalize_library`, кнопка «Вирівняти
теги за словником»): усі `ai_meta` проганяються через поточний словник без
запиту до моделі; змінені записи оновлюються в базі й у Eagle (елемент — за
`eagle_items.item_id`, без нього — за адресою поста), старі теги моделі
прибираються, ручні лишаються. На реальній базі: 46 із 189 записів.
**Звіт про словник** (`vocabulary_report`): перевикористані (>40% описів) і
мертві (0 ужитків) теги. Реальна база: `cinematic` у 49%, мертвих 235 з 367.

### Плейсхолдери інструкції

`{frames}`, `{kind}`, `{mode}`, `{taxonomy}`, `{examples}`. Підставляються `str.replace`, а
**не** `str.format`: інструкцію редагує людина, і зламана фігурна дужка не має
валити синхронізацію.

Порожній `vision_prompt` у конфізі = «взяти вбудовану». Так оновлення
застосунку покращує інструкцію тим, хто її не чіпав, і ніколи не затирає
чужу правку. Той самий трюк — з `taxonomy.json` і `LEGACY_TEMPLATES`.

---

## 8. Eagle

Локальний API на `http://localhost:41595`, живий поки програма відкрита.

**Eagle копіює файл на кожен `addFromPaths`.** Це джерело найдовше не поміченого
бага: пост, який лежить і в збережених, і в лайках, імпортувався двічі й ставав
двома елементами з двома копіями відео. Тому `eagle_one_item_per_post` типово
увімкнено, а `is_in_eagle(pk)` без підбірки відповідає «чи є будь-де».

`find_eagle_duplicates()` шукає вже накопичене за адресою поста. Карусель
дублікатом не вважає (різні файли одного поста — різні `name`). Видаляє тільки
після окремої згоди й лише в кошик; найстаршу копію лишає, бо саме в неї могли
потрапити правки користувача.

### Прибирання перевалки

`eagle_delete_local_after_import` (типово вимкнено): наприкінці проходу
локальні копії вже імпортованих постів видаляються, пост стає `archived` —
памʼять лишається, дублі не качаються. Два запобіжники:

* видаляється лише **підтверджене** — те, що реально видно в бібліотеці
  (звірка за адресою поста через `item/list`), бо копіювання на боці Eagle
  асинхронне і власна позначка «відправлено» ще не означає «лежить»;
* мовчання Eagle ≠ відсутність файлів: не вдалось отримати список — нічого
  не видаляється. Черга перегляду не чіпається ніколи.

`eagle_items.item_id` — ідентифікатор елемента Eagle. `addFromPaths`
асинхронний і id не повертає, тому він вивчається з наступного `item/list`
(у `_eagle_urls()` під час прибирання і в `describe_library`). Коли id
відомий, ретро-нормалізація й переопис читають елемент через `item/info`, а
не гортають усю бібліотеку.

Оскільки після чистки папки завантажень файли лишаються **тільки в Eagle**,
`describe_library()` бере кадри звідти: `library/info` → шлях бібліотеки,
`item/list` → елементи, `<library>/images/<id>.info/` → сам файл (шукаємо за
розширенням, бо Eagle нормалізує імена). Заодно рахує `fingerprints` для
відомих постів — так база відбитків заповнюється й для старої бібліотеки.

**Нічний бюджет** (`describe_backlog_per_run`, типово 10): наприкінці кожного
проходу `_describe_backlog()` описує ще стільки елементів бібліотеки без
опису. Бекфіл руками — багатогодинний і тому не робився; десяток за прохід
непомітний, а за місяць планових проходів покриває бібліотеку. Підбірки з
`describe_skip_collections` (галочка «Опис» у таблиці підбірок) минаються.

---

## 9. Метадані всередині файлів

Користувач попросив: «в метаданні самого відео, а не в окремий файл».

* **mp4** — iTunes-атоми через `mutagen`: `©nam`, `©ART`, `aART`, `©cmt`,
  `desc`, `ldes`, `©day`, `©gen`, `keyw`, `©alb`, `purl` і
  `----:com.apple.iTunes:URL`. Посилання дублюється в кілька полів, бо різні
  програми читають різні.
* **jpg** — EXIF через `piexif` плюс XP-поля Windows (`XPTitle`, `XPComment`,
  `XPAuthor`, `XPKeywords`) у UTF-16LE з нульовим завершенням — інакше
  провідник їх не показує.

Помилка запису тегів **ніколи** не валить завантаження: у гіршому разі файл
лишається без тегів, і про це є рядок у журналі.

---

## 10. Журнал помилок

Найцінніша частина документа: кожен пункт — реальна помилка, її причина й що
з неї слідує.

| Симптом | Корінь | Урок |
|---|---|---|
| «додало відео по грі, навіть на ревʼю не відправило» | `"art" in "gamestart"` — підрядок | Порівнюй **токени**, не підрядки (`classify.name_hit`) |
| «при відкритті ревʼю мільярд вікон і комп висне» | `resizeEvent` → `reload()` → зміна розміру → рекурсія; `mousePressEvent` відкривав файл | Перекладання з дебаунсом, смуга прокрутки завжди на місці, подвійний клік + кулдаун |
| «слайдер в самий низ улітає» | повна перебудова сітки на кожен клік | `_drop_card()` — прибрати одну картку, зберегти позицію |
| «1 постів · 2101 файлів» | `totals()` не рахував `archived` | Статус ≠ «не існує» |
| Модель мовчки не викликалась | Умова вимагала `sync_liked`, а підбірку обрали галочкою в таблиці | Не привʼязуй логіку до прапорця, який має другий шлях уводу |
| «на частину відео опису не робить» | Описувались лише НОВІ пости; бібліотека зібрана раніше | Будь-яка нова обробка потребує способу догнати старе |
| Опис із `frames=1` на відео | Нерозібране відео йшло моделі як `image/jpeg` цілим mp4 | Перевіряй тип файлу перед відправкою (`shots_from_file`) |
| «постійно одні і ті ж відоси підкачує» | Синхронізація ключувала імпорт за `pk` підбірки, обслуговування — за **назвою** | Один ключ на всю систему. Назва — для людини, pk — для коду |
| Тести зависли на 2 хв | Фейкові медіа пішли по справжніх URL | Стабай мережу в тестах явно |
| `unzip -o` на змонтованій теці | Монтування забороняє видалення | Розпакувати в `/tmp`, потім `cat` поверх |
| Кнопки спінбоксів як биті квадрати | Стилізація Qt | Прибрати кнопки зовсім (`width:0`) |
| Instagram надіслав попередження про автоматизацію | 15 проходів за 2 доби, деякі за 2 хв один від одного, кожен зі свіжим входом | Обмежувати не обсяг, а **темп**: запобіжник частоти + перевикористання сесії |
| `.bat` не запускались після подорожі через Linux | Git і `cat` лишали LF | `.gitattributes` (`*.bat eol=crlf`) — правило замість памʼяті |
| Git показав зміненими геть усі файли на диску користувача | Монтована тека віддає все як 0700, git читає це як зміну режиму | `git config core.fileMode false` у такому репозиторії |
| Дві вкладки перегляду з тими самими кнопками | Розділив за походженням запису, а не за дією над ним | Вкладки діляться за тим, що людина **робить**, а не звідки взялись дані |
| Видалений автором пост «брався» кожен прохід назавжди | Невдача лише рахувалась у `failed`, у базі сліду не лишала | Кожна невдача, яку можна повторити, потребує лічильника і межі (`gave_up`) |
| Плановий запуск у кулдауні показував вікно «синхронізація з помилками» | `cooldown` клався в `stats.errors`, а CLI будь-яку помилку вважав збоєм | Свідомий пропуск ≠ помилка: окреме поле `Stats.reason`, статус `skipped` |
| Міграція `ai_meta` губила нові стовпці | `ALTER TABLE … ADD COLUMN` виконувався до перезбирання таблиці, і стовпець зникав разом зі старою | Порядок міграцій: спершу структура, потім стовпці; і тест саме на старий макет |
| Дві резервні копії за секунду — одна | Імʼя з точністю до секунди затирало попередню | Унікальність імені перевіряти, а не сподіватись на годинник |
| Кадри з ролика з монтажем — самі переходи й чорні кадри | Рівний крок по тривалості не знає, де склейки | Вибір за змістом (сцени, яскравість), рівний крок — лише запасний |
| Репост того самого ролика — новий елемент у Eagle | Ключ — pk і хеш файлу, а перекодування міняє обидва | Перцептивний відбиток кадрів: те саме відео впізнається за картинкою |
| Нові синоніми словника не діяли в користувача | `taxonomy.json` зберігся зі старими aliases, файл повністю заміняв вбудовані | Вбудоване — підкладка під файл, а не альтернатива йому |
| Пост в Eagle без опису й тегів моделі, лише `autotagged` | У полі «Модель» стояла текстова `qwen3.8-27b`: картинки вона не бачить, а відповідь із порожніми тегами після нормалізації складалась із самого маркера — і виглядала як «є що зберегти» | Назву моделі перевіряти на візуальність (`vision.looks_visual`) до запиту і казати вголос; `has_text` не рахує маркер за текст; журнал вікна — ще й у файл `logs/gui_*.log`, інакше розбирати нічого |
| Після інсталятора «сесію не підключено» | Зібраний застосунок тримає робочі файли в `%APPDATA%\InstRef`, а запуск із вихідників — у теці проєкту; це два різні набори config/state/session | Так і задумано (Program Files не для запису), але про це має казати «Про застосунок» і README; перенесення даних — вручну, папку відкриває кнопка «Папка застосунку» |
| Бічна панель перемикалась, а сторінка — ні | `currentRowChanged` підключено до автозбереження, але не до `stack.setCurrentIndex` | Знімок кожної сторінки офскрін (`win.grab()`) перед доставкою — очима видно те, що тест на `currentRow` не ловить |
| Збірка на Windows: `UnicodeDecodeError: 'charmap'` при читанні `__init__.py` | `read_text()` без `encoding` — Windows бере cp1252, а в коді українські коментарі | Кожен `read_text`/`open` — з `encoding="utf-8"`; у CI на Windows `PYTHONUTF8=1` |
| CI червоний при «208 passed»: exit code 1 на Windows, segfault на Linux | Qt-обʼєкти з session-фікстури (вікно, трей) руйнувались після завершення інтерпретатора | Перевіряти `$?`, а не останній рядок виводу; Qt прибирати явно у фіналізаторі фікстури |
| `pip install -r requirements.txt` → ResolutionImpossible | Запінив `requests` за тим, що стояло в контейнері (2.33.1), а `instagrapi 2.18.18` вимагає ≥2.34.2; плюс запінив `imagehash`, якого код не використовує | Пін перевіряти **чистим** venv (`python -m venv` + `pip check`), а не тим середовищем, де щось уже стояло |

**Метапатерн:** майже всі баги — це «дві частини системи по-різному розуміють
одне поняття». Ключ підбірки (pk vs назва), статус файлу (немає на диску vs
не завантажено), тип медіа (відео vs картинка), порожній результат (немає vs
не питали).

---

## 10a. Збірка й поширення

Застосунок роздається як `InstRef-Setup.exe`; збирає його GitHub Actions на
`windows-latest`, бо .exe для Windows можна зібрати лише у Windows.

* `entry.py` — єдина точка входу зібраного бінара. `--sync` веде в `cli`,
  решта — у GUI. Один .exe замість двох свідомо: PySide6 з opencv важать
  ~400 МБ, і другий бінар подвоїв би інсталятор заради самої лише консолі.
* `InstRef.spec` — one-folder, не one-file: одинарний .exe розпаковував би ті
  ж 400 МБ у temp при кожному запуску. UPX вимкнено — ламає підпис Qt і
  дратує антивіруси. Зайві модулі Qt виключені (WebEngine, QML, 3D, charts).
* `installer/InstRef.iss` — Inno Setup, установка **на користувача**: без UAC,
  бо все, що застосунок пише, і так лежить у профілі.
* `.github/workflows/release.yml` — тег `v*` → тести → збірка → інсталятор і
  портативний zip у реліз.

**Специфікацію перевірено збіркою на Linux** (`pyinstaller InstRef.spec`).
Windows-бінара так не отримати, але це ловить головне, заради чого спека й
пишеться: прихований імпорт, якого бракує. Зібраний застосунок запускався в
обох режимах — `--sync` повернув очікуваний код 2 «немає сесії», GUI підняв
вікно. Розмір ~390 МБ: PySide6 99 МБ, opencv 67 МБ, numpy 42 МБ.

**Головна тонкість зібраної версії.** Встановлений застосунок лежить у Program
Files, куди користувач писати не може. Тому шляхи розділені:

* `resource_dir()` — те, що поставляється з застосунком (іконка, скрипти).
  У PyInstaller це `sys._MEIPASS`.
* `app_dir()` — куди писати. У зібраній версії `%APPDATA%\InstRef`, у
  вихідниках — корінь проєкту, як і було.

Плутанина між ними означала б застосунок, який падає при першому ж збереженні
налаштувань.

---

## 10b. Що застосунок зберігає локально

Жоден із цих файлів не належить репозиторію — усі вони в `.gitignore`:

| Файл | Що всередині | Чому чутливо |
|---|---|---|
| `session.json` | cookie `sessionid`, на Windows під DPAPI | **повний доступ до акаунта Instagram**; ключ DPAPI привʼязаний до облікового запису Windows, скопійований файл на іншій машині не читається; старий відкритий файл перешифровується при першому читанні |
| `sync.lock` | pid проходу, що йде | — |
| `backups/` | копії `state.db` (5 останніх) | те саме, що `state.db` |
| `config.json` | налаштування, токен Eagle, проксі | проксі буває з логіном і паролем |
| `device.json` | відбиток пристрою для instagrapi | привʼязаний до сесії |
| `state.db` | pk, підписи, посилання, шляхи на диску | історія того, що ти зберігав |
| `logs/` | журнали запусків | імена акаунтів і назви файлів |

`sessionid` ніде не потрапляє в журнал: у коді фігурує лише сам факт його
наявності. Але журнал містить імена авторів і назви постів, тож перед тим як
вкласти його в issue, варто переглянути.

---

## 11. Розробка

```bash
python -m pytest tests/ -q        # 209 тестів, ~14 с
ruff check igsaved tests           # лінт, той самий, що в CI
```

GUI перевіряється безголово — `tests/test_ui.py` під `QT_QPA_PLATFORM=offscreen`.
Фікстура `window` підміняє `CONFIG_PATH` і `STATE_PATH` на тимчасові й
глушить `_check_health` / `_check_updates`, щоб тест не ходив у мережу і не
писав у робочі файли проєкту.

Залежності в `requirements.txt` **запінені** точно: чужий реліз не має ламати
нашу збірку без зміни на нашому боці. Піднімати — свідомо, з прогоном суїту.

### Стиль тестів

Тести названі твердженнями про поведінку, а не про функції:
`test_skipped_post_takes_its_temporary_video_with_it`, а не `test_drop_prefetch`.
Кожен тест на виправлений баг містить у докстрінгу **симптом**, щоб через
півроку було зрозуміло, що саме він охороняє.

### Доставка на машину користувача

```bash
zip -rq update.zip . -x "*.pyc" "*__pycache__*" "taxonomy.json"
# → SendUserFile → device_commit_files у _update.zip
# → розпакувати в /tmp, cat поверх файлів (монтування блокує видалення)
# → CRLF для .bat
# → mv _update.zip _archive/IG_Saved_Sync_vN.zip
```

`.bat` **мусять** мати CRLF — інакше Windows їх не виконає.
`taxonomy.json` і `config.json` ніколи не входять в архів.

---

## 12. Історія версій

| Версія | Що зʼявилось |
|---|---|
| 1.0 | Базова синхронізація, Eagle, розклад, GUI |
| 1.0.x | Читабельні імена, метадані у файлах, іконка, розділи налаштувань |
| 1.1 | Фільтр мемів для лайків, вкладка ревʼю, списки акаунтів |
| 1.1.x | Ліміти перегляду, чистка папки зі збереженням памʼяті |
| 1.2 | Візуальна модель: кадри з усього ролика, опис і теги, редагована інструкція |
| 1.3 | Стеля кадрів 60 + автоздрібнення |
| 1.4 | Опис на кожен слайд каруселі, дозапис описів у стару бібліотеку Eagle |
| 1.5 | Контрольований словник тегів, опис англійською, зростання словника |
| 1.5.1 | Виправлено дублювання імпорту в Eagle, пошук наявних дублікатів |
| 1.5.2 | `DEVELOPMENT.md` + `CLAUDE.md`: правила розробки як частина проєкту |
| 2.0.0 | Перейменування в **InstRef**, злиття черг перегляду, інсталятор, GitHub |
| 2.1.0 | Запобіжник частоти, перевикористання сесії, спокійніші паузи |
| 2.2.0 | Межа продукту «конвеєр, не бібліотека»; прибирання перевалки після підтвердженого імпорту |
| 2.4.1 | Текстова модель у полі «Модель» тепер помічається до запиту; порожні відповіді не зберігаються; журнал вікна пишеться у файл |
| 2.4.0 | Новий інтерфейс: бічна панель із 8 розділів і підвкладками, картки стану на «Огляді», автозбереження при переході; оновлення однією кнопкою з GitHub (інсталятор або архів вихідників); `pages.py`/`widgets.py` — розбиття `main_window.py` |
| 2.3.3 | Збірка на Windows: UTF-8 при читанні версії |
| 2.3.2 | Тести: явне прибирання Qt — CI був червоний при зелених тестах |
| 2.3.1 | Виправлено піни залежностей (`requests`, зайвий `imagehash`) — 2.3.0 не встановлювався з чистого |
| 2.3.0 | Спринти 0–3 дорожньої карти разом: замок, стоп на 429 з примусовою паузою, джитер, бекапи, DPAPI, облік невдач, ротація логів; кадри за сценами й від тривалості, текст з екрана, репости за dHash, підказка підбірки, few-shot, hash інструкції, item id Eagle, ретро-нормалізація, курсор лайків, нічний бюджет описів; кадрострічка, редагування в картці, клавіатурний трияж, згода з моделлю, вхід за URL, швидкий старт, GUI-тести; перевірка оновлень, транскрипція (опційно), переопис іншою моделлю, звіт словника; запінені залежності, ruff у CI |

---

## 13. Роадмап

> Уточнено 2026-08-24 (доповнено 2026-09-02) разом із межею продукту: **InstRef — конвеєр, Eagle —
> база**. З попередньої версії роадмапу викреслено все, що будувало б другу
> бібліотеку поруч із першою: вкладку «Бібліотека», FTS-пошук, семантичний
> пошук на ембедингах, XMP для Adobe. Пошук — робота Eagle; наша робота —
> щоб йому було чим шукати: чисті теги зі словника й чесні описи.

Аналіз реальної бази (437 постів, 66 записів ai_meta) показав:

* **описано 5% бібліотеки** — бекфіл ручний і багатогодинний, тому стоїть;
* **confidence моделі — константа 0.95**: поріг мертвий, калібрування через
  промпт для малих моделей не працює; чесне калібрування — згода людини;
* **66 записів зроблено до появи словника** — брудні теги вже в Eagle,
  а збережене в ai_meta ніде не проганяється через нормалізацію повторно;
* **черга перегляду збанкрутувала**: 132 закрито гуртом, 12 розібрано;
* **`cinematic` у половині записів** — перевикористаний тег нічого не знаходить.

Другий прохід (2026-09-02, через питання «що зламається само» і «що робить
опис корисним через пів року») додав перевірене в коді:

* **невдалий пост не запам'ятовується**: `_sync_collection` лише рахує
  `failed` — видалений автором чи битий пост повторюється кожен прохід
  назавжди, витрачаючи запити до Instagram;
* **кадри беруться рівномірно, без огляду на зміст і тривалість**: у ролику з
  жорстким монтажем шість рівних кроків потрапляють у переходи й чорні кадри,
  а для тривалого туторіалу шість кадрів — майже нічого;
* **репости не ловляться**: той самий реф у трьох акаунтах — три pk і три
  різні хеші файлів (перекодування), Eagle їх теж не склеїть;
* **промпт не просить текст з екрана**: підписи в туторіалах («step 1: bake
  normals», назви плагінів) потрапляють в опис випадково;
* **у картці перегляду не можна поправити опис чи теги** — саме там, де
  видно помилку моделі, доводиться потім чистити в Eagle;
* **логи `sync_YYYY-MM.log` ростуть без обмеження**; підсумок вдалого
  планового проходу («14 нових, 3 чекають перегляду») ніде не показується —
  сповіщення є лише про помилку й мертву сесію.

### Спринт 0 — зелений реліз

> Усе, що нижче позначено [x], зроблено в 2.3.0 одним заходом (2026-09-02).
> Лишились дві речі: перший зелений реліз (потрібен push тегу — див.
> «Реліз» у CLAUDE.md) і розбиття main_window.py — рефакторинг без цінності
> для користувача, свідомо відкладений.

- [ ] **Чекає push тегу v2.3.0.** `release.yml` ще жодного разу не добіг до кінця: інсталятора не існує,
      поки збірка не пройшла. Push тегу → полагодити те, що впаде.
- [x] Запінити залежності (`instagrapi==`, `PySide6==`): відкритий верхній
      діапазон означає, що чужий реліз ламає нашу збірку без жодної нашої зміни

### Спринт 1 — міцність (захист акаунта й даних)

- [x] Міжпроцесний замок: GUI і планувальник не мають працювати одночасно
- [x] Rate-limit: `PleaseWaitFewMinutes`/429 → обірвати ВЕСЬ прохід, кулдаун 24 год
- [x] Джитер у CLI: випадкові 0–20 хв перед плановим проходом
- [x] Бекап `state.db`: перед міграцією + тижнева ротація (тримати 5)
- [x] DPAPI-шифрування session.json на Windows (ctypes, без залежностей)
- [x] Облік невдач по постах: лічильник спроб у `media`, після 3 падінь —
      статус `gave_up` із причиною і рядок у підсумку «N постів не вдалось
      узяти: …». Зараз такий пост повторюється вічно (див. аналіз вище)
- [x] Ротація логів: тримати останні 3 місяці `sync_YYYY-MM.log`

### Спринт 2 — конвеєр, який сам себе доводить до кінця

- [x] Прибирання перевалки після підтвердженого імпорту (v2.2.0)
- [x] Нічний бюджет описів: після планової синхронізації описувати N елементів
      бібліотеки (`describe_library(limit=N)` вже вміє) — покриття росте само
- [x] Ретро-нормалізація: прогнати збережені теги ai_meta через словник і
      виправити вже імпортовані елементи Eagle (`update_item`, без моделі)
- [x] Синоніми зі спостережених промахів: backlight→backlit,
      photography→photograph, motiongraphics→motion-graphics,
      blackandwhite→black-and-white, minimalist→minimalist-composition
- [x] Контекст підбірки в інструкцію: пост із «Houdini»/«Tut» описувати як
      туторіал (яку техніку показано), а не як картинку — один рядок у промпт
- [x] Хеш інструкції в `ai_meta` поруч із `model`: інакше після зміни промпта
      не дізнатись, що описано старим
- [x] Зберігати Eagle item id після імпорту: ретро-нормалізація, переопис і
      прибирання перестануть починатись із повного `item/list`
- [x] Перемикач «описувати» на підбірку: не всім підбіркам потрібна хвилина
      моделі на ролик
- [x] Лайки: курсор (найновіший pk попереднього проходу) замість вікна 50 —
      вікно мовчки губить те, що за його межею
- [x] Прев'ю з кадру відео замість CDN: Eagle робить власні, картці перегляду
      вистачить кадру — мінус запит на пост
- [x] Кількість кадрів від тривалості: ~один на 5 с у межах 6…стеля, а не
      константа для 10-секундного reel і 3-хвилинного туторіалу
- [x] Відбір кадрів за зміною сцени (різниця гістограм у OpenCV) замість
      рівних інтервалів; майже чорні кадри відкидати. Найбільший приріст
      якості опису за найменшу ціну
- [x] Поле `on_screen_text` у JSON відповіді: промпт просить виписати текст з
      екрана; він іде в анотацію Eagle і в контекст опису
- [x] Перцептивні дублікати (репости): dHash кількох кадрів при завантаженні,
      Гемінг ≤ 8 до вже імпортованого → у перегляд із підказкою «схоже на …»,
      а не тихий дубль у бібліотеці
- [x] Few-shot із власних схвалених описів: у перегляді позначка «гарний
      опис», два-три такі приклади підставляються в промпт — стиль
      вирівнюється під смак власника, той самий механізм, що й ріст словника

### Спринт 3 — перегляд, який не банкрутує

- [x] Кадрострічка на картці відео (frames.extract уже є)
- [x] Опис і теги моделі на картці
- [x] Клавіатурний трияж: стрілки + Y/N
- [x] Агрімент-статистика: зберігати (модель сказала / людина вирішила),
      точність по категоріях — єдине чесне калібрування замість confidence
- [x] GUI-тести в суїт: перевірки вікна живуть поза репозиторієм у /tmp-скриптах,
      і регресії на кшталт краху ініціалізації вкладки суїт не ловить →
      `tests/test_ui.py` (offscreen)
- [x] «Швидкий старт» на вкладці синхронізації: три індикатори
      Сесія / Eagle / Модель із кнопками — для того, хто прийшов із GitHub
- [x] Редагування опису й тегів у картці перегляду до відправки в Eagle;
      теги з автодоповненням зі словника
- [x] Другий вхід у конвеєр: вставити URL поста (або кілька) → завантаження →
      опис → Eagle. Не бібліотека — той самий пайплайн для рефів, скинутих у
      чат або яких не хочеться зберігати в Instagram
- [x] Підсумок планового проходу як сповіщення: «14 нових, 3 чекають
      перегляду» — для безголового режиму це єдиний спосіб дізнатись, що є на
      що подивитись

### Пізніше

- [x] Переописання сильнішою моделлю (колонка `model` вже є; кадри читаються
      зі сховища Eagle, локальні файли не потрібні)
- [x] Звіт словника: перевикористані (>40%) і мертві (0 ужитків) теги
- [x] Перевірка нової версії через GitHub Releases API + банер
- [x] Розбити main_window.py: pages.py (блоки) + widgets.py (помічники); tests на файли — ще ні
- [x] Транскрипція голосу за кадром (`faster-whisper`, мала модель, ЦП):
      туторіали пояснюють техніку словами, кадри цього не передають.
      Опційно, вмикати на підбірку («tutorials»); текст — у промпт і в анотацію

### Вирішує власник

* Мова інтерфейсу: UI українською при англійському README — для публічного
  репо це стеля адопції. Локалізація 2200-рядкового вікна — велика робота і
  має сенс лише якщо це продукт для інших, а не особистий інструмент із
  відкритим кодом.

### Свідомо не робимо

* Власний пошук/галерею — база в Eagle, і крапка.
* Теки в Eagle за категоріями — там є smart folders за тегами; наша робота
  дати чисті теги, розкладати — його.
* DM «надіслати собі» як джерело — ще одна поверхня приватного API одразу
  після попередження про автоматизацію.
* Імітацію людських патернів, підробку відбитків, ротацію проксі — обхід
  захисту платформи. Єдиний чесний важіль — рідше.
* Перейменування пакета `igsaved` → `instref`: зламає все, цінність нуль.
* «Unsave» після імпорту, щоб чистити збережене в Instagram — запис у їхній
  API, ще один сигнал автоматизації одразу після попередження.
* Кілька акаунтів Instagram — лише якщо власнику справді знадобиться.

**Відомі обмеження**: кукі Chrome/Edge за App-Bound Encryption — sessionid
руками; describe_library — один запит на файл, тому бекфіл іде нічним бюджетом,
а не за раз; дублікати в Eagle шукаються за адресою поста, репости — за
відбитком кадрів лише серед того, що описувалось або качалось після 2.3.0
(відбитки старої бібліотеки долічуються під час бекфілу); транскрипція
потребує `faster-whisper`, якого немає в інсталяторі.

## 14. Правило супроводу цього документа

Повний чек-лист — у `CLAUDE.md`, який читається автоматично на початку роботи
в теці проєкту. Тут — суть.

**Будь-яка зміна коду завершується оновленням цього файлу, у тій самій
зміні.** Не наприкінці спринту й не «як буде час»: документ, який відстав від
коду, шкідливіший за відсутній, бо йому вірять.

| Зробив | Онови тут |
|---|---|
| Нову можливість | розділ + рядок у таблиці версій |
| Виправив баг | **рядок у журналі помилок** (розділ 10) |
| Новий модуль | карту модулів (розділ 3) |
| Нову таблицю чи стовпець | базу даних (розділ 4) + опис міграції |
| Змінив потік роботи | схему у відповідному розділі |
| Додав поле конфігу чи тег | цифри в шапці й у розділі 7 |

**Журнал помилок наповнюється з кожного виправлення.** Три частини, і всі три
обовʼязкові: як це виглядало для користувача, у чому була справжня причина,
який урок. Рядок без урока — марний.

Числа в шапці й тексті мають бути перевірені, а не переписані по памʼяті:

```bash
python - <<'PY'
import sys; sys.path.insert(0, '.')
from dataclasses import fields
from igsaved.config import Config
from igsaved.taxonomy import Taxonomy
tax = Taxonomy()
print("полів конфігу:", len(fields(Config)))
print("категорій:", len(tax.categories), "тегів:", len(set(tax.all_tags())),
      "синонімів:", len(tax.aliases), "груп несумісних:", len(tax.exclusive))
PY
python -m pytest tests/ -q | tail -1
wc -l igsaved/*.py igsaved/ui/*.py tests/*.py | tail -1
```
